var AOMform = {
    form: document.querySelector(form),
    input: document.querySelector(id = "userNumber"),
    input: document.querySelector(id = "userExponent"),
    input: document.querySelector("userExponent").querySelector,
}

function calculateAOM(userNumber, userExponent){


}